#include <stdio.h>
#include <string.h>
#include <stdlib.h>

typedef enum { EMPTY, BLACK, WHITE, BANNED } PIECE;

typedef struct {
    int x, y;
    int player;
} Coordinate;

typedef struct {
    
} Chess;





int main(void) {

}